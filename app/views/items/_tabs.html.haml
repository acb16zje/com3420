.tabs.is-centered
  %ul
    %li{class:"#{current_class?(requests_bookings_path)}"}
      = link_to 'All Items', manager_items_path(:tab => "All", :user_id => current_user.id)
    %li{class:"#{current_class?(accepted_bookings_path)}"}
      = link_to 'Out On Booking', manager_items_path(:tab => "OnLoan", :user_id => current_user.id)
    %li{class:"#{current_class?(ongoing_bookings_path)}"}
      = link_to 'Issue', manager_items_path(:tab => "Issue", :user_id => current_user.id)
